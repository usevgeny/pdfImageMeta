# pdfImageMeta
Put script.sh and template in the same dir with JPG files in order to perform
generation of pdf files containing the image and its metadata just below. 

if needed change input files format (currently JPG) and the metadata content
    (currently takes only 14 last lines of exiftool {file} output
